Commit the changes of the repository. Create as many commits as needed, within a feature/ (or 
whatever you need) branch. Don't commit directly to main. Then create a PR from the 
created branch to main.